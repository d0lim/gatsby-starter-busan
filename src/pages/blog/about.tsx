import * as React from "react";
import Layout from "../../components/BlogLayout";

const AboutPage = ({ location }: { location: Location }) => {
  return <Layout location={location}>About Page not implemented yet</Layout>;
};

export default AboutPage;
